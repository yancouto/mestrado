\usepackage{tikz}
\usepackage{tikz-qtree}
\tikzset{>=latex}
\tikzset{edge from parent/.append style = {->, >=latex, shorten <= 2pt}}
\usetikzlibrary{matrix, backgrounds, decorations.pathreplacing, arrows, positioning, calc, fit, patterns}
